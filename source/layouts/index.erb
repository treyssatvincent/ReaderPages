<% wrap_layout :layout do %>

    <header>

        <nav>
            <a href="<%= data.config['blog']['url'] %>" class="site-title"><h1><%= data.config['blog']['title'] %></h1></a>
            <% data.config['header'].each do |link| %>
                <a href="<%= link[1] %>"><p><%= link[0] %></p></a>
            <% end %>
        </nav>

    </header>

    <%= yield %>

<% end %>
